{
  "name": "broker",
  "version": "1.0.0",
  "description": "a broker",
  "main": "server.js",
  "bin": {
    "broker": "./bin/cli.js"
  },
  "scripts": {
    "start": "node start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "stef",
  "license": "ISC",
  "dependencies": {
    "ws": "^6.1.2"
  }
}
